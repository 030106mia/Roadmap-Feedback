## Vercel / Neon / Supabase Postgres connection string
## Example:
## postgresql://USER:PASSWORD@HOST:5432/DB?schema=public
POSTGRES_PRISMA_URL=

## Vercel Blob (for image uploads)
## Create it in Vercel -> Storage -> Blob, then copy the Read/Write token.
BLOB_READ_WRITE_TOKEN=

## OpenAI (for screenshot OCR)
## IMPORTANT: do NOT commit real keys; set this in local .env / Vercel env vars.
OPENAI_API_KEY=

